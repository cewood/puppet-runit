#!<%= @shell %>

### 
## WARNING: PUPPET MANAGED FILE
##  Puppet: '<%= scope.lookupvar('::puppetversion') %>'
##  Facter: '<%= scope.lookupvar('::facterversion') %>'
##    Ruby: '<%= scope.lookupvar('::rubyversion') %>'
##  Module: '<%= scope.to_hash['module_name'] %>'
#

# Redirect stderr so everything ends up in the log file
exec 2>&1

# Output some info to log the start of this script
echo "<%= @name %> was started"

# Set environment variables
#  Maybe include this later?

# Prefix the final command with exec and ensure it runs in the foreground
exec <%= @command %>
